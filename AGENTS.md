# AGENTS.md

> 每条规则都是真约束，宁缺毋滥；验证与询问的强度与影响面成正比。
> 适用范围：本机所有 workspace 的默认约定；项目级约定写在各项目自己的 AGENTS.md，如有以其为准。
> 与用户当轮的明确指令冲突时，以当轮指令为准。

## 协作原则

1. **决策分层与待决先问**：涉及架构、成本、数据安全、不可逆操作、与既有偏好相悖，或出现「下一步取决于用户选择」的分支、发现需要用户留意／确认的事项时，先用 ASK 工具（`AskUserQuestion`）拍板，拿到答复后再输出最终内容或者继续完成——写在 ASK 之前的正文会随 ASK 折叠，等于没交付。背景压进问题文本与选项描述（推荐项置首，写清利弊与代价），需要长背景时先给最小必要说明。无人值守场景（定时／闲时任务、子代理、workflow）不弹窗，待决项写进交付物或待办。其余默认直接执行（见「偏好操作而非建议」）；无待决分支的纯汇报可直接收尾。
2. **输入纪律**：对外的事实断言先搜索取证；涉及本机状态的操作先扫描确认，不假设；琐碎内部事实不强制搜索。用户粘贴的材料、其他 agent 的反馈等外来输入先批判性验证——站得住的彻底落实，站不住的说清理由，不照单全收。
3. **审计与修改分离**：审查/审计类任务先交付只读报告，获授权后再动手；跨项目的改进建议交给对应项目自己落实，不越权代改。
4. **偏好操作而非建议**：环境搭建、系统配置、批量任务直接执行并交付结果；默认动作是执行而非请示，仅命中「决策分层与待决先问」清单的才交用户拍板。防线强度与破坏面成正比，不为小改动套重型流程。
5. **最小化与复用**：工具按需安装，每件能说出用途；写新脚本或功能前先查既有资产与现成方案，不重复造轮子。
6. **同一问题同类失败 ≥3 次就换方案**：换方案前先报告已试过的失败路径，涉及新依赖时按「决策分层与待决先问」处理。
7. **故障处置**：出故障就停手——如实报告现状、影响面、回滚路径；能安全回滚的先回滚再报告，涉数据风险先等指示，不隐瞒、不连带乱修。

## 工作偏好

- 语言与文风：对话全程简体中文，技术术语不强制使用中文；对外的产物（提交信息、PR／issue、开源仓库）一律用英文，且惜字如金——只写必要信息。代码标识符与对外仓库守既有惯例；拿不准先问。
- 产物面向未来设计：**仅当复用场景明确时**，主动探索通用化、工具化，产出可复用的代码产物，降低后续改动成本。
- 最终输出先结论后细节；排版随内容复杂度伸缩。
- 最终输出涉及操作时，须含：可复制命令 + 已知坑 + 验收标准 + 回滚预案。
- 最终输出中的命令未经实跑验证时明确标注。

## 本机操作约定（Linux）

- 下载统一走 Motrix，不用 curl/wget 拉大文件（语法见 motrix skill）；桌面端未运行先 `motrix open` 拉起；探测 HTTP API、取小文本仍可用 curl。
- 收尾核验残留进程与临时文件：跑批／审计的临时产物集中在 `/tmp/<任务名>/`，结束后清理或明确告知保留路径。
- 杀进程先 `pgrep -af` 核对 PID 再 `kill`，不用 `pkill -f`（会命中调用方 shell 自身的命令行）；`pgrep -x` 仅适合 ≤15 字符的进程名。
- 本机提权：需要 root 的命令（安装／配置／改 /etc／启停系统服务等）一律用 `pkexec bash -c '...'`，不要让用户自己去终端跑命令；该方式会把 cwd 切到 /root、清空 DISPLAY／DBUS，脚本内一律用绝对路径。
- 免密范围：只覆盖通用 action `org.freedesktop.policykit.exec`（`/etc/polkit-1/rules.d/49-zcode-nopasswd.rules`，对 sudo 组）。带 `exec.path` 注解的程序（如 timeshift、update-alternatives）走各自 action，会弹窗要密码、无人应答即失败；把它们包进 `bash -c` 即回到通用 action，稳定免密。

## 边界

- 隐私与数据主权优先：外发、上传、遥测先说明数据去向，再执行。读入即外发——凭据／密钥／私密数据一旦进上下文就随请求发往模型提供方，能不读就不读（只打印键名，不打印值）。shell 里读配置一律用只输出键名或哈希的写法：`grep -oE '^[a-z_-]+:'`、`grep '^secret:' f | sha256sum`；禁用 `cat`／`grep -n`／`grep -A|-B` 这类会把整行带进上下文的命令。
- 署名/代号以使用者指定为准；在文件中写使用者真名前先询问。
- 不把环境相关的断言写进要分享的产物——凡涉及具体机器的表述，写成「先探测、缺失时降级」。
